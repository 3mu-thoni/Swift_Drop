import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFFFF6B35);
  static const primaryDark = Color(0xFFE55A24);
  static const secondary = Color(0xFF2EC4B6);

  // Backgrounds
  static const background = Color(0xFFF8F9FA);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceDark = Color(0xFF1E1E2C);
  static const backgroundDark = Color(0xFF12121A);

  // Text
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF6B7280);
  static const textHint = Color(0xFFB0B7C3);

  // Status
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
  static const info = Color(0xFF3B82F6);

  // Order status colors
  static const statusPending = Color(0xFFF59E0B);
  static const statusConfirmed = Color(0xFF3B82F6);
  static const statusPreparing = Color(0xFF8B5CF6);
  static const statusOnTheWay = Color(0xFF2EC4B6);
  static const statusDelivered = Color(0xFF22C55E);
  static const statusCancelled = Color(0xFFEF4444);
}